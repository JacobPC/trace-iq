{
  "name": "@common-sense/trace-iq",
  "version": "0.1.0",
  "description": "Lightweight W3C traceparent + async context utilities for Node.js",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "prepare": "npm run build",
    "test": "vitest run"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "typescript": "^5.6.3",
    "@types/node": "^20.11.30"
  }
}

